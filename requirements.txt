# GNR638 Project – Python 3.11 requirements
#
# NOTE: torch / torchvision / torchaudio are installed separately in setup.bash
#       using --index-url https://download.pytorch.org/whl/cu121
#       so they are NOT listed here to avoid pip pulling the CPU-only wheel.

# ── Hugging Face stack ─────────────────────────────────────────────────────────
transformers==4.51.3
accelerate==1.6.0
huggingface_hub==0.30.2
tokenizers==0.21.1
safetensors==0.5.3
sentencepiece==0.2.0

# ── Qwen2.5-VL image utilities ─────────────────────────────────────────────────
qwen-vl-utils==0.0.10

# ── Vision / image processing ─────────────────────────────────────────────────
Pillow==10.4.0
opencv-python-headless==4.10.0.84

# ── Data handling ──────────────────────────────────────────────────────────────
pandas==2.2.3
numpy==1.26.4

# ── General utilities ──────────────────────────────────────────────────────────
tqdm==4.67.1
filelock==3.16.1
packaging==24.2
requests==2.32.3
